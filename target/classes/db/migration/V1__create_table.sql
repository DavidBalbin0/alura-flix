CREATE TABLE video (
                         id SERIAL PRIMARY KEY ,
                         titulo VARCHAR(255) NOT NULL,
                         descricao TEXT NOT NULL,
                         url TEXT NOT NULL
);